# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Virtualenv
.venv/
venv/

# Secrets — never commit real credentials
.env

# Local seen-jobs state (lives on the `state` branch in CI, not on main)
.state/

# Logs / OS
*.log
.DS_Store

# Claude Code / harness session artifacts
.claude/
