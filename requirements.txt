# Runtime dependencies, pinned to the versions verified working in this project.
# Regenerate after upgrades with:
#   .venv/bin/pip freeze | grep -iE 'jobspy|pydantic|pyyaml|rapidfuzz|jinja2|tenacity|loguru|typer|requests|pandas'
python-jobspy==1.1.82
pydantic==2.13.4
pydantic-settings==2.14.1
PyYAML==6.0.3
rapidfuzz==3.14.5
Jinja2==3.1.6
tenacity==9.1.4
loguru==0.7.3
typer==0.26.3
requests==2.34.2
# Transitive but pinned for reproducible CI (pulled in by python-jobspy):
pandas==2.3.3
